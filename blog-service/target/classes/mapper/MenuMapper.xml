<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wrr.mapper.MenuMapper">

    <select id="getMenuListByUserRole" resultType="com.wrr.entity.Menu">
        select
        a.id,
        a.menu_name,
        a.url,
        a.icon,
        a.parent_id,
        a.disabled
        from tb_menu a,tb_role_menu b,tb_role c
        where c.role_name=#{roleName} AND
              a.id=b.menu_id AND
              c.id=b.role_id

    </select>
</mapper>
